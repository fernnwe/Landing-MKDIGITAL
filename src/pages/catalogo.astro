---
import BaseLayout from "../layouts/BaseLayout.astro";
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
---

<BaseLayout title="Catálogo - MKDIGITAL Nicaragua" description="Catálogo en mantenimiento. Consulta nuestros precios por WhatsApp.">
  <Navbar />
  <main class="mnt-page">
    <canvas id="mnt-canvas"></canvas>
    <div class="mnt-bg">
      <div class="mnt-orb mnt-orb-1"></div>
      <div class="mnt-orb mnt-orb-2"></div>
      <div class="mnt-orb mnt-orb-3"></div>
      <div class="mnt-grid"></div>
      <div class="mnt-scanline"></div>
    </div>
    <section class="mnt-section">
      <div class="mnt-card">
        <div class="mnt-glow-border"></div>
        <div class="mnt-card-inner">
          <div class="mnt-status">
            <span class="mnt-dot"></span>
            <span class="mnt-status-text">SISTEMA EN MANTENIMIENTO</span>
          </div>
          <div class="mnt-icon-wrap">
            <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.066 2.573c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.573 1.066c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.066-2.573c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/><circle cx="12" cy="12" r="3"/></svg>
          </div>
          <h1>Catálogo en mantenimiento</h1>
          <p>Estamos actualizando nuestro catálogo de productos. Muy pronto tendrás disponibles todos nuestros precios actualizados.</p>
          <div class="mnt-progress">
            <div class="mnt-progress-bar"></div>
            <span class="mnt-progress-text">ACTUALIZANDO CATÁLOGO...</span>
          </div>
          <div class="mnt-sysinfo">
            <div class="mnt-sysrow">
              <span class="mnt-syslabel">ESTADO</span>
              <span class="mnt-sysvalue mnt-sysvalue--warn">EN MANTENIMIENTO</span>
            </div>
            <div class="mnt-sysrow">
              <span class="mnt-syslabel">SERVIDOR</span>
              <span class="mnt-sysvalue">MK-DIGITAL-01</span>
            </div>
            <div class="mnt-sysrow">
              <span class="mnt-syslabel">VERSIÓN</span>
              <span class="mnt-sysvalue">3.2.1</span>
            </div>
            <div class="mnt-sysrow">
              <span class="mnt-syslabel">TIEMPO ESTIMADO</span>
              <span class="mnt-sysvalue">PRÓXIMAMENTE</span>
            </div>
          </div>
          <div class="mnt-contact">
            <a href="https://wa.me/50581088124" target="_blank" rel="noopener noreferrer" class="mnt-wa">
              <svg width="22" height="22" viewBox="0 0 24 24" fill="currentColor"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>
              <span class="mnt-wa-num">+505 8108 8124</span>
              <span class="mnt-wa-label">Chatea con nosotros</span>
            </a>
            <a href="mailto:soporte.mkdigital1604@gmail.com" class="mnt-email">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
              soporte.mkdigital1604@gmail.com
            </a>
          </div>
          <div class="mnt-footer-text">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2" ry="2"/><path d="M7 11V7a5 5 0 0110 0v4"/></svg>
            MK DIGITAL &mdash; Nicaragua
          </div>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</BaseLayout>

<script>
  const canvas = document.getElementById('mnt-canvas');
  const ctx = canvas.getContext('2d');
  let w, h, particles = [];

  function resize() {
    w = canvas.width = window.innerWidth;
    h = canvas.height = window.innerHeight;
  }
  resize();
  window.addEventListener('resize', resize);

  class Particle {
    constructor() { this.reset(); }
    reset() {
      this.x = Math.random() * w;
      this.y = Math.random() * h;
      this.size = Math.random() * 1.5 + 0.5;
      this.speedX = (Math.random() - 0.5) * 0.3;
      this.speedY = (Math.random() - 0.5) * 0.3;
      this.opacity = Math.random() * 0.5 + 0.1;
    }
    update() {
      this.x += this.speedX;
      this.y += this.speedY;
      if (this.x < 0 || this.x > w || this.y < 0 || this.y > h) this.reset();
    }
    draw() {
      ctx.beginPath();
      ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(245, 158, 11, ${this.opacity})`;
      ctx.fill();
    }
  }

  for (let i = 0; i < 100; i++) particles.push(new Particle());

  function animate() {
    ctx.clearRect(0, 0, w, h);
    particles.forEach(p => { p.update(); p.draw(); });
    for (let i = 0; i < particles.length; i++) {
      for (let j = i + 1; j < particles.length; j++) {
        const dx = particles[i].x - particles[j].x;
        const dy = particles[i].y - particles[j].y;
        const dist = Math.sqrt(dx * dx + dy * dy);
        if (dist < 120) {
          ctx.beginPath();
          ctx.moveTo(particles[i].x, particles[i].y);
          ctx.lineTo(particles[j].x, particles[j].y);
          ctx.strokeStyle = `rgba(245, 158, 11, ${0.08 * (1 - dist / 120)})`;
          ctx.stroke();
        }
      }
    }
    requestAnimationFrame(animate);
  }
  animate();
</script>

<style is:global>
@import url('https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;500;600;700;800&display=swap');

#mnt-canvas { position: fixed; inset: 0; z-index: 0; pointer-events: none; }

.mnt-page {
  position: relative; min-height: 100vh; display: flex; align-items: center; justify-content: center;
  padding: 120px 20px 80px; background: var(--background); overflow: hidden;
  font-family: 'JetBrains Mono', monospace;
}

.mnt-bg { position: absolute; inset: 0; pointer-events: none; z-index: 0; }
.mnt-orb { position: absolute; border-radius: 50%; filter: blur(100px); opacity: 0.1; }
.mnt-orb-1 { width: 600px; height: 600px; background: var(--primary); top: -250px; right: -200px; animation: mnt-float 10s ease-in-out infinite; }
.mnt-orb-2 { width: 500px; height: 500px; background: var(--teal); bottom: -200px; left: -200px; animation: mnt-float 12s ease-in-out infinite reverse; }
.mnt-orb-3 { width: 350px; height: 350px; background: var(--emerald); top: 50%; left: 50%; transform: translate(-50%, -50%); animation: mnt-pulse 8s ease-in-out infinite; }
@keyframes mnt-float { 0%, 100% { transform: translateY(0) scale(1); } 50% { transform: translateY(-50px) scale(1.05); } }
@keyframes mnt-pulse { 0%, 100% { opacity: 0.05; transform: translate(-50%, -50%) scale(1); } 50% { opacity: 0.15; transform: translate(-50%, -50%) scale(1.2); } }
.mnt-grid { position: absolute; inset: 0; background-image: linear-gradient(rgba(255,255,255,0.025) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.025) 1px, transparent 1px); background-size: 50px 50px; }
.mnt-scanline { position: absolute; left: 0; right: 0; height: 2px; background: linear-gradient(90deg, transparent, rgba(245,158,11,0.3), transparent); animation: scan 4s linear infinite; opacity: 0.5; }
@keyframes scan { 0% { top: -2px; } 100% { top: 100%; } }

.mnt-section { position: relative; z-index: 2; text-align: center; max-width: 480px; width: 100%; }
.mnt-card { position: relative; border-radius: 20px; padding: 1px; overflow: hidden; }
.mnt-glow-border { position: absolute; inset: -1px; border-radius: 20px; background: conic-gradient(from 0deg, transparent, rgba(245,158,11,0.4), rgba(6,182,212,0.4), rgba(16,185,129,0.4), transparent); animation: mnt-rotate 4s linear infinite; mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0); -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0); mask-composite: exclude; -webkit-mask-composite: xor; padding: 1px; }
@keyframes mnt-rotate { to { transform: rotate(360deg); } }
.mnt-card-inner { position: relative; background: rgba(10,10,14,0.92); backdrop-filter: blur(40px); border-radius: 19px; padding: 48px 36px; display: flex; flex-direction: column; align-items: center; gap: 12px; border: 1px solid rgba(255,255,255,0.04); }

.mnt-status { display: flex; align-items: center; gap: 8px; margin-bottom: 4px; }
.mnt-dot { width: 8px; height: 8px; border-radius: 50%; background: #f59e0b; box-shadow: 0 0 12px rgba(245,158,11,0.6); animation: mnt-blink 1.4s ease-in-out infinite; }
@keyframes mnt-blink { 0%, 100% { opacity: 1; } 50% { opacity: 0.3; } }
.mnt-status-text { font-size: 10px; font-weight: 600; letter-spacing: 3px; color: var(--primary); }

.mnt-icon-wrap { width: 64px; height: 64px; display: flex; align-items: center; justify-content: center; border-radius: 16px; background: rgba(245,158,11,0.06); color: var(--primary); border: 1px solid rgba(245,158,11,0.12); }
.mnt-card-inner h1 { font-size: 1.5rem; font-weight: 700; color: var(--on-surface); margin: 0; letter-spacing: -0.3px; }
.mnt-card-inner > p { font-size: 0.82rem; color: var(--on-surface-variant); margin: 0 0 4px; line-height: 1.7; max-width: 360px; font-family: var(--font-family); }

.mnt-progress { width: 100%; max-width: 260px; display: flex; flex-direction: column; align-items: center; gap: 6px; margin: 4px 0 16px; }
.mnt-progress-bar { width: 100%; height: 3px; background: rgba(255,255,255,0.06); border-radius: 3px; overflow: hidden; position: relative; }
.mnt-progress-bar::after { content: ''; position: absolute; top: 0; left: -40%; width: 40%; height: 100%; background: linear-gradient(90deg, transparent, var(--primary), transparent); border-radius: 3px; animation: mnt-loading 1.8s ease-in-out infinite; }
@keyframes mnt-loading { 0% { left: -40%; } 100% { left: 100%; } }
.mnt-progress-text { font-size: 10px; color: var(--on-surface-variant); letter-spacing: 1px; opacity: 0.6; }

.mnt-sysinfo { width: 100%; border: 1px solid rgba(255,255,255,0.04); border-radius: 10px; padding: 12px 16px; display: flex; flex-direction: column; gap: 6px; background: rgba(0,0,0,0.2); }
.mnt-sysrow { display: flex; justify-content: space-between; align-items: center; }
.mnt-syslabel { font-size: 9px; letter-spacing: 1.5px; color: rgba(255,255,255,0.25); font-weight: 500; }
.mnt-sysvalue { font-size: 10px; color: rgba(255,255,255,0.45); font-weight: 500; }
.mnt-sysvalue--warn { color: var(--primary); }

.mnt-contact { display: flex; flex-direction: column; gap: 8px; width: 100%; margin-top: 4px; }
.mnt-wa { display: flex; align-items: center; gap: 14px; padding: 14px 20px; border-radius: 12px; background: rgba(37,211,102,0.1); border: 1px solid rgba(37,211,102,0.2); text-decoration: none; transition: all 0.3s; }
.mnt-wa:hover { background: rgba(37,211,102,0.18); border-color: rgba(37,211,102,0.4); transform: translateY(-1px); }
.mnt-wa svg { color: #25D366; flex-shrink: 0; }
.mnt-wa-num { font-size: 0.95rem; font-weight: 700; color: var(--on-surface); font-family: 'JetBrains Mono', monospace; }
.mnt-wa-label { font-size: 10px; color: var(--on-surface-variant); margin-left: auto; opacity: 0.6; font-family: var(--font-family); }
.mnt-email { display: flex; align-items: center; justify-content: center; gap: 8px; padding: 10px 20px; border-radius: 10px; text-decoration: none; color: var(--on-surface-variant); font-size: 0.78rem; transition: all 0.3s; border: 1px solid transparent; }
.mnt-email svg { color: var(--primary); flex-shrink: 0; }
.mnt-email:hover { color: var(--primary); border-color: rgba(245,158,11,0.15); background: rgba(245,158,11,0.04); }

.mnt-footer-text { display: flex; align-items: center; gap: 6px; margin-top: 8px; font-size: 9px; color: rgba(255,255,255,0.15); letter-spacing: 0.5px; font-family: var(--font-family); }
.mnt-footer-text svg { color: rgba(255,255,255,0.1); }

@media (max-width: 600px) { .mnt-card-inner { padding: 36px 20px; } .mnt-card-inner h1 { font-size: 1.2rem; } .mnt-wa { flex-wrap: wrap; gap: 8px; } .mnt-wa-label { margin-left: 0; width: 100%; text-align: center; } }
</style>
