/* =============================================
   CATALOGO SECTION — Professional Redesign
   ============================================= */

.catalogo { padding-top: 64px; }

/* ===== HERO ===== */
.catalogo-hero {
  position: relative;
  padding: 100px 0 80px;
  overflow: hidden;
  background:
    linear-gradient(180deg, var(--surface-container-low) 0%, var(--background) 100%);
}
.catalogo-hero::before {
  content: '';
  position: absolute; inset: 0;
  background:
    radial-gradient(ellipse at 20% 0%, rgba(6, 182, 212, 0.08) 0%, transparent 50%),
    radial-gradient(ellipse at 80% 100%, rgba(129, 140, 248, 0.06) 0%, transparent 50%);
  pointer-events: none;
}
.catalogo-hero-bg {
  position: absolute; inset: 0;
  overflow: hidden;
  pointer-events: none;
}
.hero-orb {
  position: absolute; border-radius: 50%;
  filter: blur(120px); opacity: 0.35;
  animation: orb-float 25s ease-in-out infinite;
}
.orb-1 {
  width: 700px; height: 700px;
  top: -350px; left: -200px;
  background: radial-gradient(circle, rgba(6, 182, 212, 0.15) 0%, transparent 70%);
}
.orb-2 {
  width: 500px; height: 500px;
  bottom: -250px; right: -150px;
  background: radial-gradient(circle, rgba(129, 140, 248, 0.12) 0%, transparent 70%);
  animation-delay: -12s;
}

.hero-content {
  position: relative; z-index: 1;
  text-align: center;
  max-width: 720px;
  margin: 0 auto;
}

.hero-content .badge {
  margin-bottom: 18px;
  border-color: rgba(6, 182, 212, 0.3);
  color: var(--primary-bright);
  background: linear-gradient(135deg, rgba(6, 182, 212, 0.1), rgba(129, 140, 248, 0.08));
  animation: badge-pulse 2.5s ease-in-out infinite;
}
@keyframes badge-pulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(6, 182, 212, 0.2); }
  50% { box-shadow: 0 0 0 16px rgba(6, 182, 212, 0); }
}

.hero-content h1 {
  font-family: var(--font-display);
  font-size: clamp(36px, 5vw, 54px);
  font-weight: 800;
  line-height: 1.08;
  margin-bottom: 20px;
  letter-spacing: -0.03em;
  background: linear-gradient(135deg, var(--on-surface) 0%, var(--primary-bright) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.hero-content .text-accent {
  display: inline;
}

.hero-content > p {
  font-size: clamp(16px, 1.8vw, 19px);
  color: var(--on-surface-variant);
  max-width: 520px;
  margin: 0 auto 28px;
  line-height: 1.7;
}

/* Trust indicators */
.hero-trust {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 16px 24px;
  margin-top: 36px;
  padding-top: 24px;
  border-top: 1px solid var(--outline-variant);
}

.trust-item {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 8px 16px;
  border-radius: 9999px;
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid var(--outline-variant);
  font-size: 13px;
  font-weight: 500;
  color: var(--on-surface-variant);
  transition: all 0.25s ease;
}
.trust-item:hover {
  border-color: var(--primary);
  background: rgba(6, 182, 212, 0.08);
  color: var(--primary-bright);
}
.trust-item svg { width: 15px; height: 15px; color: var(--primary); }

/* Search & Filters Bar */
.search-filters {
  position: relative; z-index: 5;
  margin: 0 auto 64px;
  padding: 28px 36px;
  max-width: 1120px;
  border-radius: 22px;
  background: linear-gradient(135deg, rgba(255,255,255,0.03), rgba(255,255,255,0.015));
  border: 1px solid var(--outline-variant);
  backdrop-filter: blur(20px);
  box-shadow: 0 8px 32px -12px rgba(0,0,0,0.4);
}

.search-row {
  display: flex;
  gap: 16px;
  align-items: center;
  flex-wrap: wrap;
  margin-bottom: 20px;
}

.search-bar {
  position: relative;
  flex: 1;
  min-width: 280px;
  max-width: 420px;
}
.search-bar svg {
  position: absolute; left: 18px; top: 50%; transform: translateY(-50%);
  width: 20px; height: 20px;
  color: var(--on-surface-variant);
  pointer-events: none;
  transition: color 0.25s;
}
.search-bar:focus-within svg { color: var(--primary); }
.search-bar input {
  width: 100%;
  padding: 14px 18px 14px 52px;
  border-radius: 12px;
  background: var(--surface-container-high);
  border: 1.5px solid var(--outline-variant);
  color: var(--on-surface);
  font-size: 15px;
  font-family: var(--font-family);
  outline: none;
  transition: border-color 0.25s, box-shadow 0.25s, background 0.25s;
}
.search-bar input::placeholder { color: var(--on-surface-variant); opacity: 0.6; }
.search-bar:focus-within input {
  border-color: var(--primary);
  box-shadow: 0 0 0 4px rgba(6, 182, 212, 0.12);
  background: var(--surface);
}

.filter-chips {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  justify-content: center;
}

.filter-chip {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  border-radius: 9999px;
  background: var(--surface-container-high);
  border: 1.5px solid var(--outline-variant);
  color: var(--on-surface-variant);
  font-size: 13.5px;
  font-weight: 500;
  cursor: pointer;
  white-space: nowrap;
  transition: all 0.25s cubic-bezier(0.2, 0, 0, 1);
}
.filter-chip:hover {
  border-color: var(--primary);
  color: var(--on-surface);
  background: rgba(6, 182, 212, 0.06);
}
.filter-chip.active {
  background: linear-gradient(135deg, var(--primary), #0891B2);
  border-color: transparent;
  color: var(--on-primary);
  box-shadow: 0 4px 16px rgba(6, 182, 212, 0.3);
}
.filter-chip .chip-icon { display: inline-flex; color: var(--cat-color); }
.filter-chip .chip-icon svg { width: 16px; height: 16px; }
.filter-chip.active .chip-icon { color: var(--on-primary); }

/* Results count */
.results-count {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 10px 0 4px;
  font-size: 14px;
  color: var(--on-surface-variant);
}
.results-count strong { color: var(--primary-bright); font-family: var(--font-display); }

/* Category Sections */
.catalogo-body {
  background: var(--surface-container);
  padding: 0 0 100px;
}

.cat-section {
  margin-bottom: 64px;
}
.cat-section:last-child { margin-bottom: 0; }

.cat-header {
  display: flex;
  align-items: center;
  gap: 20px;
  margin-bottom: 28px;
  padding: 20px 24px;
  border-radius: 18px;
  background: linear-gradient(135deg, rgba(255,255,255,0.02), transparent);
  border: 1px solid var(--outline-variant);
  cursor: default;
  transition: border-color 0.3s, box-shadow 0.3s;
}
.cat-header:hover {
  border-color: var(--cat-color);
  box-shadow: 0 0 24px -8px var(--cat-color);
}

.cat-icon {
  width: 56px; height: 56px; min-width: 56px;
  display: flex; align-items: center; justify-content: center;
  border-radius: 14px;
  box-shadow: 0 6px 20px rgba(0,0,0,0.25);
}
.cat-icon svg { width: 28px; height: 28px; }

.cat-meta {
  flex: 1;
}

.cat-title {
  font-size: 20px;
  font-weight: 700;
  color: var(--on-surface);
  margin: 0 0 4px;
  letter-spacing: -0.01em;
}

.cat-count {
  font-size: 13px;
  font-weight: 500;
  color: var(--primary-bright);
  margin: 0;
}

/* Products Grid — 4 columns on large, responsive */
.productos-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 24px;
}

.product-wrapper { animation: fade-up 0.5s ease both; }
@keyframes fade-up {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

/* Stagger animation for grid items */
.product-wrapper:nth-child(1) { animation-delay: 0ms; }
.product-wrapper:nth-child(2) { animation-delay: 60ms; }
.product-wrapper:nth-child(3) { animation-delay: 120ms; }
.product-wrapper:nth-child(4) { animation-delay: 180ms; }
.product-wrapper:nth-child(5) { animation-delay: 240ms; }
.product-wrapper:nth-child(6) { animation-delay: 300ms; }
.product-wrapper:nth-child(7) { animation-delay: 360ms; }
.product-wrapper:nth-child(8) { animation-delay: 420ms; }
.product-wrapper:nth-child(9) { animation-delay: 480ms; }
.product-wrapper:nth-child(10) { animation-delay: 540ms; }
.product-wrapper:nth-child(11) { animation-delay: 600ms; }
.product-wrapper:nth-child(12) { animation-delay: 660ms; }

/* Empty state */
.empty-state {
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 100px 24px;
  text-align: center;
  gap: 16px;
}
.empty-state svg { width: 64px; height: 64px; color: var(--on-surface-variant); opacity: 0.25; }
.empty-state h3 { font-size: 22px; font-weight: 700; color: var(--on-surface); margin: 0; }
.empty-state p { font-size: 15px; color: var(--on-surface-variant); margin: 0 0 16px; }
.empty-state .btn-outline { padding: 12px 28px; font-weight: 600; }

/* ===== Responsive ===== */
@media (max-width: 1400px) {
  .productos-grid { grid-template-columns: repeat(3, 1fr); gap: 20px; }
}

@media (max-width: 1024px) {
  .catalogo-hero { padding: 80px 0 60px; }
  .catalogo-body { padding: 0 0 80px; }
  .search-filters { padding: 24px 28px; margin-bottom: 48px; }
  .productos-grid { grid-template-columns: repeat(2, 1fr); gap: 16px; }
  .cat-header { padding: 16px 20px; gap: 16px; }
  .cat-icon { width: 48px; height: 48px; }
  .cat-title { font-size: 18px; }
}

@media (max-width: 768px) {
  .catalogo-hero { padding: 64px 0 48px; }
  .catalogo-body { padding: 0 0 64px; }
  .search-filters { padding: 20px; margin-bottom: 36px; }
  .search-row { flex-direction: column; align-items: stretch; }
  .search-bar { max-width: none; }
  .filter-chips { overflow-x: auto; justify-content: flex-start; padding-bottom: 4px; }
  .filter-chip { white-space: nowrap; }
  .productos-grid { grid-template-columns: 1fr; gap: 14px; }
  .cat-section { margin-bottom: 40px; }
}

@media (max-width: 480px) {
  .hero-content h1 { font-size: 32px; }
  .search-filters { padding: 16px; }
  .filter-chip { padding: 8px 16px; font-size: 12.5px; }
}

/* ===== Light theme ===== */
html[data-theme='light'] .catalogo-hero {
  background: linear-gradient(180deg, #F7F9FC 0%, #F2F5F9 100%);
}
html[data-theme='light'] .catalogo-body { background: #F2F5F9; }
html[data-theme='light'] .search-filters {
  background: linear-gradient(135deg, rgba(255,255,255,0.9), rgba(255,255,255,0.7));
  border-color: #E2E8F0;
}
html[data-theme='light'] .search-bar input {
  background: #fff; border-color: #E2E8F0;
}
html[data-theme='light'] .search-bar:focus-within input {
  border-color: var(--primary);
  box-shadow: 0 0 0 4px rgba(6, 182, 212, 0.15);
  background: #fff;
}
html[data-theme='light'] .filter-chip {
  background: #fff; border-color: #E2E8F0;
}
html[data-theme='light'] .filter-chip:hover {
  background: rgba(6, 182, 212, 0.08);
}
html[data-theme='light'] .cat-header {
  background: linear-gradient(135deg, rgba(15,23,42,0.02), transparent);
  border-color: #E2E8F0;
}
html[data-theme='light'] .cat-header:hover {
  border-color: var(--cat-color);
  box-shadow: 0 0 24px -8px var(--cat-color);
}
html[data-theme='light'] .trust-item {
  background: #fff; border-color: #E2E8F0;
}
html[data-theme='light'] .trust-item:hover {
  border-color: var(--primary);
  background: rgba(6, 182, 212, 0.06);
}

/* Cart button in catalog */
.cart-btn-catalogo {
  position: relative;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 48px;
  height: 48px;
  min-width: 48px;
  border-radius: 14px;
  background: linear-gradient(135deg, var(--primary), #0891B2);
  border: none;
  color: var(--on-primary);
  cursor: pointer;
  transition: transform 0.25s, box-shadow 0.25s, background 0.25s;
  flex-shrink: 0;
}
.cart-btn-catalogo:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 24px -4px rgba(6, 182, 212, 0.5);
}
.cart-btn-catalogo:active { transform: translateY(0) scale(0.98); }

.cart-btn-catalogo .cart-badge {
  position: absolute;
  top: -4px;
  right: -4px;
  min-width: 20px;
  height: 20px;
  padding: 0 6px;
  border-radius: 10px;
  background: #EF4444;
  color: #fff;
  font-size: 11px;
  font-weight: 700;
  font-family: var(--font-mono);
  display: flex;
  align-items: center;
  justify-content: center;
  border: 2px solid var(--surface);
  animation: badge-pop 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
}
@keyframes badge-pop {
  0% { transform: scale(0); opacity: 0; }
  50% { transform: scale(1.2); }
  100% { transform: scale(1); opacity: 1; }
}

html[data-theme='light'] .cart-btn-catalogo {
  background: linear-gradient(135deg, var(--primary), #0891B2);
}