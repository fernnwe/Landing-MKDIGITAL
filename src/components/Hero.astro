<section class="hero" id="inicio">
  <div class="hero-bg-grid"></div>

  <div class="container hero-grid">

    <!-- IZQUIERDA -->
    <div class="hero-content">

      <span class="hero-badge">
        SOPORTE REMOTO EN TODA NICARAGUA
      </span>

      <h1>
        Soporte informático remoto  
        <span>rápido y profesional</span>
      </h1>

      <p class="hero-desc">
        Instalamos programas, activamos licencias, eliminamos virus y optimizamos tu PC 
        sin que salgas de casa. Atención inmediata y segura.
      </p>

      <!-- BOTONES -->
      <div class="hero-buttons">
        <a href="https://wa.me/50581088124" target="_blank" class="btn-primary">
          Solicitar soporte
        </a>

        <a href="/catalogo" class="btn-secondary">
          Ver catálogo
        </a>
      </div>

      <!-- BENEFICIOS -->
      <div class="hero-features">
        <span>⚡ Atención inmediata</span>
        <span>🔒 Instalación segura</span>
        <span>💳 Pago por transferencia</span>
        <span>🖥️ Soporte remoto real</span>
      </div>

      <!-- STATS -->
      <div class="hero-stats">
        <div class="stat">
          <strong>+500</strong>
          <span>Clientes atendidos</span>
        </div>

        <div class="stat">
          <strong>15 min</strong>
          <span>Tiempo de respuesta</span>
        </div>

        <div class="stat">
          <strong>100%</strong>
          <span>Remoto</span>
        </div>
      </div>

    </div>

    <!-- DERECHA -->
    <div class="hero-card">
      <div class="card-glow"></div>

      <div class="card-content">
        <h3>Servicios más solicitados</h3>

        <ul>
          <li>💻 Instalación de programas</li>
          <li>⚙️ Drivers y optimización</li>
          <li>🛡️ Eliminación de virus</li>
          <li>📦 Instalación de Office</li>
        </ul>

        <div class="status">
          🟢 Disponibles 24/7
        </div>
      </div>
    </div>

  </div>
</section>

<style>

/* ===== HERO BASE ===== */
.hero{
  position:relative;
  min-height:100vh;
  display:flex;
  align-items:center;
  padding-top:110px;
  overflow:hidden;

  background:
    radial-gradient(circle at 20% 20%,#00b3c933,transparent 40%),
    radial-gradient(circle at 80% 80%,#1ed6e633,transparent 40%);
}

/* GRID BG */
.hero-bg-grid{
  position:absolute;
  inset:0;
  background-image:
    linear-gradient(rgba(255,255,255,0.04) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px);
  background-size:40px 40px;
  mask-image:radial-gradient(circle at center, black 30%, transparent 70%);
}

/* GRID */
.hero-grid{
  display:grid;
  grid-template-columns: 1.1fr .9fr;
  gap:80px;
  align-items:center;
}

/* BADGE */
.hero-badge{
  display:inline-block;
  padding:8px 18px;
  border-radius:30px;
  background:linear-gradient(90deg,#00B3C920,#1ED6E620);
  color:var(--primary);
  font-size:.75rem;
  letter-spacing:.7px;
  margin-bottom:20px;
}

/* TITULO */
.hero h1{
  font-size:64px;
  line-height:1.05;
  margin-bottom:22px;
}

.hero h1 span{
  color:var(--primary);
  text-shadow:0 0 25px #00b3c980;
}

/* DESC */
.hero-desc{
  font-size:22px;
  color:var(--muted);
  margin-bottom:35px;
  max-width:520px;
}

/* BOTONES */
.hero-buttons{
  display:flex;
  gap:20px;
  margin-bottom:28px;
}

.btn-primary{
  padding:16px 32px;
  border-radius:40px;
  text-decoration:none;
  font-weight:600;
  color:white;
  background:linear-gradient(90deg,#00B3C9,#1ED6E6);
  box-shadow:0 10px 30px #00b3c955;
  transition:.3s;
}

.btn-primary:hover{
  transform:translateY(-4px);
}

.btn-secondary{
  padding:16px 30px;
  border-radius:40px;
  text-decoration:none;
  border:1px solid rgba(255,255,255,0.2);
  color:white;
  transition:.3s;
}

.btn-secondary:hover{
  background:rgba(255,255,255,0.08);
}

/* FEATURES */
.hero-features{
  display:flex;
  gap:20px;
  flex-wrap:wrap;
  color:var(--muted);
  font-size:.9rem;
  margin-bottom:30px;
}

/* STATS */
.hero-stats{
  display:flex;
  gap:45px;
}

.stat strong{
  display:block;
  font-size:30px;
  color:var(--primary);
}

.stat span{
  font-size:.8rem;
  color:var(--muted);
}

/* CARD */
.hero-card{
  position:relative;
}

.card-glow{
  position:absolute;
  inset:-30px;
  background:linear-gradient(120deg,#00B3C9,#1ED6E6);
  filter:blur(90px);
  opacity:.35;
}

.card-content{
  position:relative;
  padding:45px;
  border-radius:28px;
  background:rgba(255,255,255,0.05);
  backdrop-filter:blur(16px);
  border:1px solid rgba(255,255,255,0.08);
  box-shadow:0 25px 60px rgba(0,0,0,.4);
  transition:.4s;
}

.card-content:hover{
  transform:translateY(-10px);
}

.card-content ul{
  list-style:none;
  padding:0;
  margin:20px 0;
}

.card-content li{
  margin-bottom:12px;
  color:var(--muted);
}

.status{
  margin-top:15px;
  font-weight:600;
  color:#32ff8f;
}

/* ===== MOBILE PERFECTO ===== */
@media(max-width:900px){

  .hero-grid{
    grid-template-columns:1fr;
    text-align:center;
    gap:50px;
  }

  .hero h1{
    font-size:42px;
  }

  .hero-desc{
    margin:auto auto 30px;
  }

  .hero-buttons{
    justify-content:center;
  }

  .hero-features{
    justify-content:center;
  }

  .hero-stats{
    justify-content:center;
  }

}

</style>