<section class="hero" id="inicio">
  <div class="hero-bg-grid"></div>

  <div class="container hero-grid">

    <!-- IZQUIERDA -->
    <div class="hero-content">

      <span class="hero-badge">
        <i class="fa-solid fa-wifi"></i>
        SOPORTE REMOTO EN TODA NICARAGUA
      </span>

      <h1>
        Soporte informático remoto  
        <span>rápido y profesional</span>
      </h1>

      <p class="hero-desc">
        Instalamos programas, configuramos, eliminamos virus y optimizamos tu PC 
        sin que salgas de casa. Atención inmediata y segura.
      </p>

      <div class="hero-buttons">
        <a href="https://wa.me/50581088124" target="_blank" class="btn-primary">
          <i class="fa-brands fa-whatsapp"></i>
          Solicitar soporte
        </a>

        <a href="/catalogo" class="btn-secondary">
          <i class="fa-solid fa-layer-group"></i>
          Ver catálogo
        </a>
      </div>

      <!-- FEATURES -->
      <div class="hero-features">
        <div class="feature"><i class="fa-solid fa-bolt"></i> Atención inmediata</div>
        <div class="feature"><i class="fa-solid fa-lock"></i> Instalación segura</div>
        <div class="feature"><i class="fa-solid fa-credit-card"></i> Pago por transferencia</div>
        <div class="feature"><i class="fa-solid fa-desktop"></i> Soporte remoto real</div>
      </div>

      <!-- STATS -->
      <div class="hero-stats">
        <div class="stat">
          <i class="fa-solid fa-users"></i>
          <strong>+500</strong>
          <span>Clientes atendidos</span>
        </div>

        <div class="stat">
          <i class="fa-solid fa-clock"></i>
          <strong>15 min</strong>
          <span>Tiempo de respuesta</span>
        </div>

        <div class="stat">
          <i class="fa-solid fa-globe"></i>
          <strong>100%</strong>
          <span>Remoto</span>
        </div>
      </div>

    </div>

    <!-- DERECHA -->
<div class="hero-card">
  <div class="card-glow"></div>

  <div class="card-content">

    <div class="card-header">
      <h3>Servicios más solicitados</h3>
      <span class="card-badge">En linea</span>
    </div>

    <div class="services-list">

      <div class="service-item">
        <div class="icon">
          <i class="fa-solid fa-laptop"></i>
        </div>
        <div class="service-text">
          <strong>Instalación de programas</strong>
          <span>Office, Adobe, AutoCAD y más</span>
        </div>
      </div>

      <div class="service-item">
        <div class="icon">
          <i class="fa-solid fa-microchip"></i>
        </div>
        <div class="service-text">
          <strong>Drivers y optimización</strong>
          <span>Máximo rendimiento del sistema</span>
        </div>
      </div>

      <div class="service-item">
        <div class="icon">
          <i class="fa-solid fa-shield-virus"></i>
        </div>
        <div class="service-text">
          <strong>Eliminación de virus</strong>
          <span>Limpieza profunda y protección</span>
        </div>
      </div>

      <div class="service-item">
        <div class="icon">
          <i class="fa-solid fa-file-word"></i>
        </div>
        <div class="service-text">
          <strong>Instalación de Office</strong>
          <span>Activación completa y lista</span>
        </div>
      </div>

    </div>

  </div>
</div>

  </div>
</section>

<style>

.hero{
  position:relative;
  min-height:100vh;
  display:flex;
  align-items:center;
  padding-top:110px;
  overflow:hidden;
  background:
    radial-gradient(circle at 20% 20%,#00b3c933,transparent 40%),
    radial-gradient(circle at 80% 80%,#1ed6e633,transparent 40%);
}

/* GRID BACKGROUND */
.hero-bg-grid{
  position:absolute;
  inset:0;
  background-image:
    linear-gradient(rgba(255,255,255,0.04) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px);
  background-size:40px 40px;
  mask-image:radial-gradient(circle at center, black 30%, transparent 70%);
}

.hero-grid{
  display:grid;
  grid-template-columns:1.1fr .9fr;
  gap:80px;
  align-items:center;
}

/* =========================================================
   BADGE
   ========================================================= */

.hero-badge{
  display:inline-flex;
  align-items:center;
  gap:10px;
  padding:8px 18px;
  border-radius:30px;
  background:linear-gradient(90deg,#00B3C920,#1ED6E620);
  color:var(--primary);
  font-size:.75rem;
  margin-bottom:20px;
}

/* =========================================================
   TITULO Y TEXTO
   ========================================================= */

.hero h1{
  font-size:64px;
  line-height:1.05;
  margin-bottom:22px;
}

.hero h1 span{
  color:var(--primary);
  text-shadow:0 0 25px #00b3c980;
}

.hero-desc{
  font-size:22px;
  color:var(--muted);
  margin-bottom:35px;
  max-width:520px;
}

/* =========================================================
   BOTONES
   ========================================================= */

.hero-buttons{
  display:flex;
  gap:18px;
  margin-bottom:30px;
}

.btn-primary{
  display:flex;
  align-items:center;
  gap:10px;
  padding:16px 30px;
  border-radius:40px;
  text-decoration:none;
  font-weight:600;
  color:white;
  background:linear-gradient(90deg,#00B3C9,#1ED6E6);
  box-shadow:0 10px 30px #00b3c955;
  transition:.3s;
}

.btn-primary:hover{
  transform:translateY(-4px);
}

.btn-secondary{
  display:flex;
  align-items:center;
  gap:10px;
  padding:16px 28px;
  border-radius:40px;
  text-decoration:none;
  border:1px solid rgba(255,255,255,0.2);
  color:white;
  transition:.3s;
}

.btn-secondary:hover{
  background:rgba(255,255,255,0.08);
}

/* =========================================================
   FEATURES CHIPS
   ========================================================= */

.hero-features{
  display:flex;
  flex-wrap:wrap;
  gap:14px;
  margin-bottom:35px;
}

.feature{
  display:flex;
  align-items:center;
  gap:8px;
  background:rgba(255,255,255,.05);
  padding:10px 16px;
  border-radius:30px;
  font-size:.85rem;
  color:var(--muted);
}

/* =========================================================
   STATS
   ========================================================= */

.hero-stats{
  display:flex;
  gap:35px;
}

.stat{
  display:flex;
  flex-direction:column;
  gap:5px;
}

.stat i{
  color:var(--primary);
  margin-bottom:4px;
}

.stat strong{
  font-size:28px;
  color:var(--primary);
}

.stat span{
  font-size:.8rem;
  color:var(--muted);
}

/* =========================================================
   HERO CARD PREMIUM (SERVICIOS)
   ========================================================= */

.hero-card{
  position:relative;
}

.card-glow{
  position:absolute;
  inset:-40px;
  background:linear-gradient(120deg,#00B3C9,#1ED6E6);
  filter:blur(90px);
  opacity:.35;
}

.card-content{
  position:relative;
  padding:45px;
  border-radius:28px;
  background:rgba(255,255,255,0.05);
  backdrop-filter:blur(16px);
  border:1px solid rgba(255,255,255,0.08);
  box-shadow:0 25px 60px rgba(0,0,0,.45);
}

/* HEADER CARD */
.card-header{
  display:flex;
  justify-content:space-between;
  align-items:center;
  margin-bottom:25px;
}

.card-header h3{
  font-size:22px;
}

.card-badge{
  background:#00b3c920;
  color:var(--primary);
  padding:6px 14px;
  border-radius:20px;
  font-size:.75rem;
  font-weight:600;
}

/* LISTA SERVICIOS */
.services-list{
  display:flex;
  flex-direction:column;
  gap:18px;
}

.service-item{
  display:flex;
  gap:16px;
  padding:16px;
  border-radius:14px;
  transition:.25s;
  border:1px solid transparent;
}

.service-item:hover{
  background:rgba(255,255,255,.05);
  border:1px solid rgba(255,255,255,.08);
  transform:translateX(6px);
}

.service-item .icon{
  min-width:42px;
  height:42px;
  display:flex;
  align-items:center;
  justify-content:center;
  border-radius:12px;
  background:linear-gradient(120deg,#00B3C920,#1ED6E620);
  color:var(--primary);
  font-size:18px;
}

.service-text strong{
  display:block;
  font-size:15px;
}

.service-text span{
  font-size:.8rem;
  color:var(--muted);
}

/* STATUS */
.status{
  margin-top:25px;
  display:flex;
  align-items:center;
  gap:10px;
  font-weight:600;
  color:#32ff8f;
}

.dot{
  width:10px;
  height:10px;
  background:#32ff8f;
  border-radius:50%;
  box-shadow:0 0 10px #32ff8f;
}

/* =========================================================
   MOBILE PERFECTO
   ========================================================= */

@media(max-width:900px){

  .hero-grid{
    grid-template-columns:1fr;
    text-align:center;
    gap:50px;
  }

  .hero h1{
    font-size:40px;
  }

  .hero-desc{
    margin:auto auto 30px;
  }

  .hero-buttons{
    justify-content:center;
    flex-direction:column;
    align-items:center;
  }

  .hero-features{
    justify-content:center;
  }

  .hero-stats{
    justify-content:center;
  }

  .hero-card{
    display:flex;
    justify-content:center;
    width:100%;
  }

  .card-content{
    width:100%;
    max-width:360px;
    margin:auto;
    padding:35px 28px;
  }

  .card-header{
    flex-direction:column;
    gap:10px;
    text-align:center;
  }

}
</style>