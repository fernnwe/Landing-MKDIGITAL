<section class="hero" id="inicio">
  <div class="container hero-grid">

    <!-- LEFT -->
    <div class="hero-content">
      <span class="badge">
        <i class="fa-solid fa-wifi"></i>
        SOPORTE REMOTO EN TODA NICARAGUA
      </span>

      <h1>
        Soporte informático remoto  
        <span>rápido y profesional</span>
      </h1>

      <p class="hero-desc">
        Instalamos programas, configuramos, eliminamos virus y optimizamos tu PC 
        sin que salgas de casa. Atención inmediata y segura.
      </p>

      <div class="hero-buttons">
        <a href="https://wa.me/50581088124" target="_blank" class="btn-primary btn-lg">
          <i class="fa-brands fa-whatsapp"></i>
          Solicitar soporte
        </a>
        <a href="/catalogo" class="btn-outline btn-lg">
          <i class="fa-solid fa-layer-group"></i>
          Ver catálogo
        </a>
      </div>

      <div class="hero-features">
        <span class="chip"><i class="fa-solid fa-bolt"></i> Atención inmediata</span>
        <span class="chip"><i class="fa-solid fa-lock"></i> Instalación segura</span>
        <span class="chip"><i class="fa-solid fa-credit-card"></i> Pago por transferencia</span>
        <span class="chip"><i class="fa-solid fa-desktop"></i> Soporte remoto real</span>
      </div>

      <div class="hero-stats">
        <div class="stat-item">
          <i class="fa-solid fa-users"></i>
          <strong>+500</strong>
          <span>Clientes atendidos</span>
        </div>
        <div class="stat-item">
          <i class="fa-solid fa-clock"></i>
          <strong>15 min</strong>
          <span>Tiempo de respuesta</span>
        </div>
        <div class="stat-item">
          <i class="fa-solid fa-globe"></i>
          <strong>100%</strong>
          <span>Remoto</span>
        </div>
      </div>
    </div>

    <!-- RIGHT -->
    <div class="hero-card-wrapper">
      <div class="hero-card">
        <div class="hero-card-header">
          <h3>Servicios más solicitados</h3>
          <span class="status-chip">En linea</span>
        </div>

        <div class="hero-services">
          <div class="hero-service-item">
            <div class="service-icon">
              <i class="fa-solid fa-laptop"></i>
            </div>
            <div class="service-text">
              <strong>Instalación de programas</strong>
              <span>Office, Adobe, AutoCAD y más</span>
            </div>
          </div>

          <div class="hero-service-item">
            <div class="service-icon">
              <i class="fa-solid fa-microchip"></i>
            </div>
            <div class="service-text">
              <strong>Drivers y optimización</strong>
              <span>Máximo rendimiento del sistema</span>
            </div>
          </div>

          <div class="hero-service-item">
            <div class="service-icon">
              <i class="fa-solid fa-shield-virus"></i>
            </div>
            <div class="service-text">
              <strong>Eliminación de virus</strong>
              <span>Limpieza profunda y protección</span>
            </div>
          </div>

          <div class="hero-service-item">
            <div class="service-icon">
              <i class="fa-solid fa-file-word"></i>
            </div>
            <div class="service-text">
              <strong>Instalación de Office</strong>
              <span>Activación completa y lista</span>
            </div>
          </div>
        </div>
      </div>
    </div>

  </div>
</section>

<style>
/* MD3 Hero Section */
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  padding-top: 64px;
  overflow: hidden;
  background: linear-gradient(180deg, var(--surface-container-low) 0%, var(--background) 100%);
}

.hero-grid {
  display: grid;
  grid-template-columns: 1.1fr 0.9fr;
  gap: 64px;
  align-items: center;
  padding-top: 48px;
  padding-bottom: 48px;
}

/* Left content */
.hero-content .badge { margin-bottom: 16px; }

.hero h1 {
  font-size: clamp(32px, 4vw, 48px);
  font-weight: 700;
  line-height: 1.1;
  margin-bottom: 20px;
}
.hero h1 span { color: var(--primary); }

.hero-desc {
  font-size: 16px;
  color: var(--on-surface-variant);
  margin-bottom: 32px;
  max-width: 480px;
  line-height: 1.6;
}

.hero-buttons {
  display: flex;
  gap: 12px;
  margin-bottom: 32px;
}

.btn-lg {
  padding: 12px 28px;
  font-size: 15px;
}

/* MD3 Chips */
.chip {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 14px;
  border-radius: var(--shape-full);
  background: var(--surface-container-high);
  border: 1px solid var(--outline-variant);
  color: var(--on-surface-variant);
  font-size: 12px;
  font-weight: 500;
}
.chip i { font-size: 12px; color: var(--primary); }

.hero-features {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-bottom: 32px;
}

/* Stats row */
.hero-stats {
  display: flex;
  gap: 32px;
}

.stat-item {
  display: flex;
  flex-direction: column;
  gap: 2px;
}
.stat-item i { color: var(--primary); font-size: 14px; margin-bottom: 2px; }
.stat-item strong { font-size: 24px; font-weight: 700; color: var(--on-surface); }
.stat-item span { font-size: 12px; color: var(--on-surface-variant); }

/* Right card - MD3 Elevated Card */
.hero-card-wrapper {
  position: relative;
}

.hero-card {
  background: var(--surface-container-high);
  border-radius: var(--shape-xl);
  box-shadow: var(--elevation-3);
  padding: 32px;
  border: 1px solid rgba(255,255,255,0.06);
}

.hero-card-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
}

.hero-card-header h3 { font-size: 20px; font-weight: 600; }

.status-chip {
  padding: 4px 12px;
  border-radius: var(--shape-full);
  background: rgba(0, 200, 83, 0.15);
  color: #4ADE80;
  font-size: 11px;
  font-weight: 600;
  display: flex;
  align-items: center;
  gap: 6px;
}
.status-chip::before {
  content: '';
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: #4ADE80;
}

/* Service items */
.hero-services {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.hero-service-item {
  display: flex;
  gap: 14px;
  align-items: center;
  padding: 12px;
  border-radius: var(--shape-md);
  transition: background 0.2s;
  cursor: default;
}
.hero-service-item:hover {
  background: rgba(255,255,255,0.04);
}

.service-icon {
  width: 40px;
  height: 40px;
  min-width: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--shape-sm);
  background: var(--primary-container);
  color: var(--primary);
  font-size: 16px;
}

.service-text strong {
  display: block;
  font-size: 14px;
  font-weight: 600;
  color: var(--on-surface);
}
.service-text span {
  font-size: 12px;
  color: var(--on-surface-variant);
}

@media (max-width: 900px) {
  .hero-grid {
    grid-template-columns: 1fr;
    gap: 48px;
    text-align: center;
  }

  .hero-desc { margin: 0 auto 32px; }
  .hero-buttons { justify-content: center; flex-direction: column; align-items: center; }
  .hero-features { justify-content: center; }
  .hero-stats { justify-content: center; }
  .hero-card-wrapper { display: flex; justify-content: center; }
  .hero-card { width: 100%; max-width: 400px; }
  .hero-card-header { flex-direction: column; gap: 8px; text-align: center; }
  .stat-item { align-items: center; }
}
</style>
