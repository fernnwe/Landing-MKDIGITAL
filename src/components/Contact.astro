<section id="contacto" class="section contact-section">
  <div class="container contact-grid">

    <!-- TEXTO -->
    <div class="contact-info">
      <span class="badge">CONTACTO</span>
      <h2>¿Listo para instalar tu software?</h2>

      <p class="contact-desc">
        Todos nuestros servicios se realizan de forma remota.
        Escríbenos por WhatsApp y comenzamos hoy mismo.
      </p>

      <div class="contact-cards">
        <div class="mini-card">⚡ Atención rápida</div>
        <div class="mini-card">💻 Soporte remoto</div>
        <div class="mini-card">🔒 Instalación segura</div>
      </div>
    </div>

    <!-- TARJETA -->
    <div class="contact-box">
      <div class="whatsapp-icon">💬</div>

      <h3>Hablar por WhatsApp</h3>
      <p class="contact-sub">Respuesta rápida en minutos</p>

      <a 
        href="https://wa.me/50581088124?text=Hola,%20quiero%20instalar%20software"
        target="_blank"
        class="whatsapp-btn"
      >
        Escribir ahora →
      </a>

      <span class="note">Horario: 6am - 9pm • Hora Nicaragua</span>
    </div>

  </div>
</section>

<script is:inline>
/* Animación al aparecer */
const contactObserver = new IntersectionObserver(entries=>{
  entries.forEach(entry=>{
    if(entry.isIntersecting){
      entry.target.classList.add("show-contact");
    }
  });
},{threshold:.2});

contactObserver.observe(document.querySelector(".contact-section"));
</script>

<style>

/* ANIMACIÓN DE ENTRADA */
.contact-section{
  opacity:0;
  transform:translateY(70px);
  transition:all .9s ease;
  position:relative;
  padding:120px 0;
  background:
    radial-gradient(circle at 15% 20%, #00B3C915 0%, transparent 40%),
    radial-gradient(circle at 85% 80%, #1ED6E615 0%, transparent 40%);
}

.show-contact{
  opacity:1;
  transform:translateY(0);
}

/* GRID */
.contact-grid{
  display:grid;
  grid-template-columns:1.1fr 1fr;
  gap:80px;
  align-items:center;
}

/* IZQUIERDA */
.contact-info h2{
  font-size:44px;
  margin:15px 0;
}

.contact-desc{
  color:var(--muted);
  font-size:1.1rem;
  line-height:1.7;
  max-width:520px;
}

.badge{
  display:inline-block;
  background:linear-gradient(90deg,#00B3C920,#1ED6E620);
  color:var(--primary);
  padding:7px 18px;
  border-radius:30px;
  font-size:.75rem;
  letter-spacing:.5px;
}

/* MINI BADGES */
.contact-cards{
  display:flex;
  gap:15px;
  margin-top:35px;
  flex-wrap:wrap;
}

.mini-card{
  background:rgba(255,255,255,0.05);
  border:1px solid rgba(255,255,255,0.08);
  padding:13px 20px;
  border-radius:30px;
  font-size:.9rem;
  backdrop-filter:blur(10px);
  transition:.3s;
}

.mini-card:hover{
  border-color:var(--primary);
}

/* TARJETA */
.contact-box{
  padding:60px 45px;
  border-radius:32px;
  text-align:center;
  background:rgba(255,255,255,0.05);
  backdrop-filter: blur(18px);
  border:1px solid rgba(255,255,255,0.08);
  box-shadow:0 30px 70px rgba(0,0,0,.35);
  position:relative;
  overflow:hidden;
  will-change:transform;
}

.contact-box:hover{
  transform:translateY(-10px);
}

/* glow animado liviano */
.contact-box::before{
  content:"";
  position:absolute;
  inset:0;
  background:linear-gradient(120deg,transparent,#00B3C940,transparent);
  opacity:.5;
}

/* ICONO */
.whatsapp-icon{
  width:85px;
  height:85px;
  margin:auto auto 22px;
  border-radius:50%;
  display:flex;
  align-items:center;
  justify-content:center;
  font-size:36px;
  background:linear-gradient(135deg,#00B3C930,#1ED6E630);
  box-shadow:0 0 30px #00b3c940;
}

.contact-sub{
  color:var(--muted);
  margin-bottom:25px;
}

/* BOTÓN */
.whatsapp-btn{
  display:block;
  text-decoration:none;
  font-weight:700;
  padding:17px;
  border-radius:40px;
  background:linear-gradient(90deg,#00B3C9,#1ED6E6);
  color:white;
  transition:.3s;
  box-shadow:0 10px 30px #00b3c950;
}

.whatsapp-btn:hover{
  transform:scale(1.06);
  box-shadow:0 18px 45px #00b3c980;
}

.note{
  display:block;
  margin-top:18px;
  font-size:.8rem;
  color:var(--muted);
}

/* MOBILE */
@media(max-width:900px){
  .contact-grid{
    grid-template-columns:1fr;
    text-align:center;
    gap:50px;
  }

  .contact-info h2{
    font-size:32px;
  }
}

</style>