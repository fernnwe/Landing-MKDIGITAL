<section id="contacto" class="section contact-section">
  <div class="container contact-grid">

    <!-- LEFT -->
    <div class="contact-info">

      <span class="badge">CONTACTO</span>

      <h2>
        ¿Listo para resolver tu problema  
        <span>en minutos?</span>
      </h2>

      <p class="contact-desc">
        Conéctate con un técnico ahora mismo y comenzamos el soporte remoto.
        Instalamos, optimizamos y dejamos tu computadora funcionando hoy.
      </p>

      <div class="contact-benefits">

        <div class="contact-benefit">
          <i class="fa-solid fa-clock"></i>
          <div>
            <strong>Respuesta inmediata</strong>
            <span>Tiempo promedio: 15 minutos</span>
          </div>
        </div>

        <div class="contact-benefit">
          <i class="fa-solid fa-laptop"></i>
          <div>
            <strong>Soporte 100% remoto</strong>
            <span>Sin salir de casa</span>
          </div>
        </div>

        <div class="contact-benefit">
          <i class="fa-solid fa-shield-halved"></i>
          <div>
            <strong>Proceso seguro</strong>
            <span>Conexión protegida</span>
          </div>
        </div>

      </div>

      <div class="contact-trust">
        <i class="fa-solid fa-circle-check"></i>
        Más de <strong>500 clientes atendidos</strong> en Nicaragua
      </div>

    </div>

    <!-- RIGHT -->
    <div class="contact-card-wrapper">

      <div class="contact-card">

        <div class="contact-wa-icon">
          <i class="fa-brands fa-whatsapp"></i>
        </div>

        <h3>Hablar por WhatsApp</h3>
        <p class="contact-sub">Disponible de 6:00 AM a 9:00 PM</p>

        <a 
          href="https://wa.me/50581088124?text=Hola,%20quiero%20instalar%20software"
          target="_blank"
          class="contact-wa-btn"
        >
          Iniciar conversación
          <i class="fa-solid fa-arrow-right"></i>
        </a>

        <div class="contact-chips">
          <span><i class="fa-solid fa-bolt"></i> Atención rápida</span>
          <span><i class="fa-solid fa-lock"></i> Conexión segura</span>
          <span><i class="fa-solid fa-desktop"></i> Soporte remoto real</span>
        </div>

      </div>
    </div>

  </div>
</section>

<style>
/* MD3 Contact Section */
.contact-section {
  padding: 96px 0;
  background: var(--surface-container, #17191E);
}

.contact-grid {
  display: grid;
  grid-template-columns: 1.1fr 0.9fr;
  gap: 64px;
  align-items: center;
}

.contact-info h2 {
  font-size: clamp(28px, 3.5vw, 40px);
  font-weight: 700;
  line-height: 1.1;
  margin: 16px 0 16px;
}
.contact-info h2 span { color: var(--primary); }

.contact-desc {
  font-size: 15px;
  max-width: 480px;
  margin-bottom: 32px;
  line-height: 1.6;
}

.contact-benefits {
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.contact-benefit {
  display: flex;
  gap: 14px;
  align-items: flex-start;
}
.contact-benefit i {
  font-size: 18px;
  color: var(--primary);
  margin-top: 2px;
  width: 24px;
}
.contact-benefit strong {
  display: block;
  font-size: 14px;
  font-weight: 600;
  color: var(--on-surface);
}
.contact-benefit span {
  font-size: 12px;
  color: var(--on-surface-variant);
}

.contact-trust {
  margin-top: 24px;
  font-size: 13px;
  color: var(--on-surface-variant);
}
.contact-trust i { color: var(--primary); margin-right: 6px; }
.contact-trust strong { color: var(--on-surface); }

/* Right card */
.contact-card-wrapper { position: relative; }

.contact-card {
  background: var(--surface-container-high, #1C1D22);
  border-radius: var(--shape-xl);
  padding: 40px 32px;
  text-align: center;
  box-shadow: var(--elevation-2);
  border: 1px solid rgba(255,255,255,0.06);
  transition: box-shadow 0.2s ease;
}
.contact-card:hover { box-shadow: var(--elevation-3); }

.contact-wa-icon {
  width: 72px;
  height: 72px;
  margin: 0 auto 20px;
  border-radius: 50%;
  background: var(--primary-container);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 32px;
  color: var(--primary);
}

.contact-card h3 {
  font-size: 20px;
  font-weight: 600;
  margin-bottom: 4px;
}

.contact-sub {
  font-size: 13px;
  color: var(--on-surface-variant);
  margin-bottom: 24px;
}

.contact-wa-btn {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 10px;
  padding: 14px 24px;
  border-radius: var(--shape-full);
  background: var(--primary);
  color: var(--on-primary);
  font-size: 15px;
  font-weight: 600;
  text-decoration: none;
  margin-bottom: 24px;
  box-shadow: var(--elevation-1);
  transition: box-shadow 0.2s ease;
}
.contact-wa-btn:hover { box-shadow: var(--elevation-2); }

.contact-chips {
  display: flex;
  justify-content: center;
  gap: 12px;
  flex-wrap: wrap;
}
.contact-chips span {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
  color: var(--on-surface-variant);
}
.contact-chips i { color: var(--primary); font-size: 12px; }

@media (max-width: 900px) {
  .contact-grid {
    grid-template-columns: 1fr;
    text-align: center;
    gap: 48px;
  }
  .contact-desc { margin: 0 auto 32px; }
  .contact-benefits { align-items: center; }
  .contact-benefit { justify-content: center; }
}
</style>
