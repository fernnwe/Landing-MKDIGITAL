<section class="urgency">
  <div class="marquee">
    <div class="track">
      <span>🔥 PROMOCIÓN DE LA SEMANA — 20% OFF EN INSTALACIONES</span>
      <span>⚡ Cupos limitados por día</span>
      <span>💻 Soporte remoto inmediato en toda Nicaragua</span>
      <span>🛡️ Garantía incluida en todos los servicios</span>
      <span>🔥 PROMOCIÓN DE LA SEMANA — 20% OFF EN INSTALACIONES</span>
      <span>⚡ Cupos limitados por día</span>
      <span>💻 Soporte remoto inmediato en toda Nicaragua</span>
      <span>🛡️ Garantía incluida en todos los servicios</span>
    </div>
  </div>

  <div class="cta">
    <a href="https://wa.me/50581088124" target="_blank" class="cta-btn">
      <span>Solicitar instalación ahora</span>
      <svg viewBox="0 0 24 24">
        <path d="M13 5l7 7-7 7M5 12h15"/>
      </svg>
    </a>
  </div>
</section>

<style>
.urgency{
  background:#0b0f14;
  border-bottom:1px solid rgba(255,255,255,0.08);
  overflow:hidden;
}

/* TEXTO EN MOVIMIENTO */
.marquee{
  width:100%;
  overflow:hidden;
  white-space:nowrap;
  border-bottom:1px solid rgba(255,255,255,0.05);
}

.track{
  display:inline-flex;
  gap:70px;
  padding:14px 0;
  animation:scroll 28s linear infinite;
  font-weight:600;
  color:#00e0ff;
  letter-spacing:.3px;
}

@keyframes scroll{
  from{ transform:translateX(0); }
  to{ transform:translateX(-50%); }
}

/* CTA CONTENEDOR */
.cta{
  text-align:center;
  padding:18px 0 20px;
}

/* BOTÓN PREMIUM */
.cta-btn{
  display:inline-flex;
  align-items:center;
  gap:12px;
  padding:14px 28px;
  border-radius:14px;
  text-decoration:none;
  font-weight:800;
  font-size:15px;
  color:#eaffff;
  position:relative;
  overflow:hidden;

  background:rgba(255,255,255,0.05);
  backdrop-filter:blur(10px);
  border:1px solid rgba(0,224,255,.35);

  transition:.35s;
}

/* brillo animado */
.cta-btn::before{
  content:"";
  position:absolute;
  inset:0;
  background:linear-gradient(120deg,transparent,rgba(0,224,255,.25),transparent);
  opacity:0;
  transition:.4s;
}

.cta-btn:hover::before{
  opacity:1;
}

/* hover general */
.cta-btn:hover{
  transform:translateY(-2px) scale(1.05);
  box-shadow:0 0 25px rgba(0,224,255,.25);
  border-color:#00ffa6;
}

/* flecha */
.cta-btn svg{
  width:18px;
  height:18px;
  stroke:#00e0ff;
  stroke-width:2;
  fill:none;
  transition:.3s;
}

.cta-btn:hover svg{
  transform:translateX(5px);
  stroke:#00ffa6;
}
</style>