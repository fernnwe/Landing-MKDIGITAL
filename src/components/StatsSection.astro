<section class="stats">
  <div class="container">

    <div class="stats-header">
      <span class="badge pulse">RESULTADOS REALES</span>
      <h2>Nuestro impacto en números</h2>
      <p>Empresas y profesionales confían cada día en nuestro soporte remoto.</p>
    </div>

    <div class="stats-grid">

      <div class="stat-card">
        <div class="number" data-target="500">0</div>
        <div class="label">Instalaciones realizadas</div>
      </div>

      <div class="stat-card">
        <div class="number" data-target="300">0</div>
        <div class="label">Clientes atendidos</div>
      </div>

      <div class="stat-card">
        <div class="number rating">4.9</div>
        <div class="label">Calificación promedio</div>
      </div>

      <div class="stat-card">
        <div class="number">100%</div>
        <div class="label">Cobertura nacional</div>
      </div>

    </div>

  </div>
</section>

<script is:inline>
const counters = document.querySelectorAll('.number[data-target]');
let started = false;

const animateCounters = () => {
  if(started) return;
  started = true;

  counters.forEach(counter => {
    const target = +counter.dataset.target;
    const duration = 1500;
    let start = 0;
    const increment = target / (duration / 16);

    const updateCount = () => {
      start += increment;
      if(start < target){
        counter.innerText = Math.floor(start);
        requestAnimationFrame(updateCount);
      } else {
        counter.innerText = "+" + target;
      }
    };
    updateCount();
  });
};

const observer = new IntersectionObserver(entries => {
  if(entries[0].isIntersecting) animateCounters();
});

observer.observe(document.querySelector('.stats'));
</script>

<style>
.stats{
  padding:150px 0;
  background:
    radial-gradient(circle at 20% 20%,#00b3c920,transparent 40%),
    radial-gradient(circle at 80% 80%,#1ed6e620,transparent 40%),
    #070b10;
  text-align:center;
  position:relative;
  overflow:hidden;
}

/* glow hero */
.stats::before{
  content:"";
  position:absolute;
  width:700px;
  height:700px;
  background:radial-gradient(circle,#00B3C925,transparent 70%);
  top:-250px;
  left:50%;
  transform:translateX(-50%);
  filter:blur(140px);
}

/* HEADER */
.stats-header{
  margin-bottom:80px;
  position:relative;
}

.badge{
  display:inline-block;
  padding:7px 18px;
  border-radius:30px;
  background:linear-gradient(90deg,#00B3C920,#1ED6E620);
  color:var(--primary);
  font-size:.75rem;
  letter-spacing:.7px;
  margin-bottom:18px;
  font-weight:700;
}

/* pulse hero color */
.pulse{
  animation:pulse 2s infinite;
}
@keyframes pulse{
  0%{box-shadow:0 0 0 0 #00b3c955}
  70%{box-shadow:0 0 0 12px transparent}
  100%{box-shadow:0 0 0 0 transparent}
}

.stats-header h2{
  font-size:46px;
  margin-bottom:12px;
}

.stats-header p{
  color:var(--muted);
  max-width:520px;
  margin:auto;
}

/* GRID */
.stats-grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(240px,1fr));
  gap:40px;
}

/* CARD */
.stat-card{
  background:rgba(255,255,255,0.04);
  border:1px solid rgba(255,255,255,0.08);
  border-radius:26px;
  padding:50px 25px;
  backdrop-filter:blur(16px);
  transition:.4s;
  position:relative;
}

.stat-card:hover{
  transform:translateY(-14px);
  border-color:var(--primary);
  box-shadow:0 0 45px #00b3c930;
}

/* NUMERO HERO GRADIENT */
.number{
  font-size:58px;
  font-weight:900;
  background:linear-gradient(90deg,#00B3C9,#1ED6E6);
  -webkit-background-clip:text;
  -webkit-text-fill-color:transparent;
  margin-bottom:12px;
  letter-spacing:-1px;
}

/* TEXTO */
.label{
  color:var(--muted);
  font-size:15px;
}

@media(max-width:768px){
  .stats{ padding:120px 0; }
  .stats-header h2{ font-size:34px; }
}
</style>